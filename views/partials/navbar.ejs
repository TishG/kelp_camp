<nav class="navbar navbar-expand-lg navbar-light bg-light">
    
    <section class="container">
        <a class="navbar-brand" href="/">KelpCamp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#myNavbar" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="fas fa-angle-down"></span>
        </button>
        <section class="collapse navbar-collapse" id="myNavbar">
        <section class="container text-center d-flex justify-content-center justify-content-lg-end">
            <section class="navbar-nav">
                <a class="nav-item nav-link active" href="/">Home <span class="sr-only">(current)</span></a>
                <a class="nav-item nav-link" href="/campgrounds">Campgrounds</a>
                <% if(!currentUser) { %>
                <a class="nav-item nav-link" href="/login">Login</a>
                <a class="nav-item nav-link btn btn-primary text-white" href="/register">Sign Up</a>
                <% } else { %>
                <a class="nav-item nav-link" href="/users/<%= currentUser._id %>">Profile</a>
                <a class="nav-item nav-link" href="#">( Signed in as <strong><%= currentUser.username %></strong> )</a>
                <!-- <% if(currentUser && currentUser.isAdmin) { %>
                    <a class="nav-item nav-link text-primary" href="#">Admin user</a>
                <% } %> -->
                <a class="nav-item nav-link" href="/logout">Logout</a>
                <% } %>
            </section>
        </section>
        </section>

    </section>
</nav>